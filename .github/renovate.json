{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "customManagers:dockerfileVersions",
    "customManagers:githubActionsVersions",
    ":semanticCommits",
    ":dependencyDashboard"
  ],
  "timezone": "Etc/UTC",
  "labels": ["dependencies"],
  "rangeStrategy": "bump",
  "automerge": true,
  "automergeStrategy": "squash",
  "platformAutomerge": true,
  "ignoreDeps": ["nextcloud"],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Track GitHub Actions wrapped by Wandalen/wretry.action. Renovate's github-actions manager only reads `uses:`, not the nested `action:` input wretry takes, so without this the retry-wrapped actions (setup-buildx, checkout, login, …) would silently stop receiving updates.",
      "fileMatch": ["^\\.github/workflows/.+\\.ya?ml$"],
      "matchStrings": ["action:\\s+(?<depName>[A-Za-z0-9._-]+/[A-Za-z0-9._-]+)@(?<currentValue>[A-Za-z0-9._+-]+)"],
      "datasourceTemplate": "github-tags"
    }
  ],
  "packageRules": [
    {
      "description": "Group all GitHub Actions bumps into one PR.",
      "matchManagers": ["github-actions"],
      "groupName": "github-actions"
    },
    {
      "description": "nginx + supervisor are pinned in the Dockerfile and tracked via the `deb` datasource (markers there). Repology strips the Debian -N+debNNuM revision so it can't see security rebuilds; the deb datasource reads the actual package index. Point it at both trixie (base) and trixie-security so security revisions are picked up — Renovate merges the registries and takes the highest. All apt bumps grouped into one PR.",
      "matchDatasources": ["deb"],
      "registryUrls": [
        "https://deb.debian.org/debian?suite=trixie&components=main&binaryArch=amd64",
        "https://deb.debian.org/debian-security?suite=trixie-security&components=main&binaryArch=amd64"
      ],
      "groupName": "debian-apt"
    },
    {
      "description": "Standalone bumps for tool versions tracked in build.yml (crane, etc.).",
      "matchDatasources": ["github-releases", "github-tags"],
      "matchFileNames": [".github/workflows/**"]
    },
    {
      "description": "Major bumps require manual review — surface as checkboxes on the Dependency Dashboard, do not open the PR automatically, do not auto-merge. Minor/patch still flow through automerge gated by all-ci.",
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "dependencyDashboardApproval": true
    }
  ]
}
