; Test fixture bind-mounted at /usr/local/etc/php/conf.d/99-local.ini.
; Used by tests/bats/22-bind-mount-overrides.bats to verify that user-supplied
; PHP overrides take precedence over both upstream defaults and our env-driven
; zz-env-overrides.ini.

date.timezone = America/Anchorage
opcache.jit_buffer_size = 384M
